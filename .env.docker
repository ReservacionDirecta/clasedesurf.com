# ==============================================
# SURF SCHOOL - DOCKER ENVIRONMENT VARIABLES
# ==============================================

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=surfschool_secure_password_2024
POSTGRES_DB=clasedesurf.com
POSTGRES_PORT=5432

# Application Ports
FRONTEND_PORT=3000
BACKEND_PORT=4000
REDIS_PORT=6379
HTTP_PORT=80
HTTPS_PORT=443
EVOLUTION_PORT=8080

# Security & Authentication
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production
NEXTAUTH_SECRET=your-super-secure-nextauth-secret-change-this-in-production
REDIS_PASSWORD=redis_secure_password_2024

# Application URLs
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXTAUTH_URL=http://localhost:3000

# Environment
NODE_ENV=production

# WhatsApp Integration (Optional)
WHATSAPP_ENABLED=false
EVOLUTION_API_KEY=change-this-api-key-for-production
EVOLUTION_INSTANCE_NAME=surfschool
EVOLUTION_SERVER_URL=http://localhost:8080
WEBHOOK_BASE_URL=http://backend:4000
LOG_LEVEL=INFO

# ==============================================
# PRODUCTION NOTES:
# 1. Change all passwords and secrets
# 2. Update URLs to your domain
# 3. Set NODE_ENV=production
# 4. Enable HTTPS in nginx configuration
# 5. Set WHATSAPP_ENABLED=true if needed
# ==============================================